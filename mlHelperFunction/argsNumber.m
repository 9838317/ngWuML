%this function easily sums all the (parameters[i] .* parameters[i])

function [ result ] = argsNumber( varargin )

    lenArgVec = length(varargin);
    disp(varargin);
    disp(varargin(3:end));
    result = 0;

end

